const numInches = 72;
const conversionFactor = 2.54;

const numCentimeters = numInches * conversionFactor;

const myOutput = `${numInches} inches is equivalent to ${numCentimeters} centimeters.`;
console.log(myOutput)